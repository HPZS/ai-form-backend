# 项目规则

本仓库是 AI 智能录入助手的后端，与插件仓库 `ai-auto-form` 配套。

- 遵循插件仓库根目录的《通用兼容工程守则.md》：**禁止静默失败**（不吞 error、不无日志降级）、**日志完备性**（仅凭日志能重建执行路径）、诚实报错、稳定性第一。
- 涉及账务（积分预占/扣费/退还、支付回调）的改动，必须先写能复现问题的测试再修；失败路径要落状态、留根因。
- 产品开发按插件仓库的《docs/产品开发计划.md》推进。
- 部署铁律见 `docs/部署指南.md`：镜像只在开发机构建并推 Docker Hub，服务器只 pull，禁止在服务器上构建或 clone 源码。
- 交付流程：明确的修改需求连续完成全部开发、验证和部署，不拆成等待确认的阶段；每个功能点完成后提交代码，最终 push。后端改动完成后直接构建、推送镜像并更新已配置的生产服务器，同步有变化的提示词并核验服务；凭据不写入仓库。
