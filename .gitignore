# 二进制与构建产物
/bin/
*.exe
*.test
*.out

# 私有运行时配置(绝不入库);.env.example 是无密钥模板,应入库
/config/private/
.env
.env.*
!.env.example

# 注:prompts/private/ 曾经不入库(避免公开提示词)。2026-08-18 起改为随仓库提交——
# 提示词不入库带来的代价更大:没有版本历史(改坏了没法 diff、没法回滚)、
# 没有备份源(全世界只有开发机与服务器各一份)、换开发机后端直接起不来
# (LoadPrompts 缺文件即 log.Fatalf)。本仓库是公开的 AGPL 项目,提交即公开发布,
# 这是权衡后的选择。密钥仍然绝不入库(.env 一行未动)。

# 本地数据库与日志
*.db
*.sqlite
/logs/

# 前端(dist 随仓库提交以便 go:embed 直接构建)
web/node_modules/

# IDE
.idea/
.vscode/
